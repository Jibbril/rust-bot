#[allow(dead_code)]
pub enum CalculationMode {
    Close,
    #[allow(dead_code)] // TODO: Remove once used
    Open,
    #[allow(dead_code)] // TODO: Remove once used
    High,
    #[allow(dead_code)] // TODO: Remove once used
    Low,
}
